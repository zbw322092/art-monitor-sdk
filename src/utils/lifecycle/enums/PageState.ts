export enum PageState {
  'ACTIVE' = 'active',
  'PASSIVE' = 'passive',
  'HIDDEN' = 'hidden',
  'FROZEN' = 'frozen',
  // 'DISCARDED' = 'discarded', Not used but show to completeness.
  'TERMINATED' = 'terminated',
}